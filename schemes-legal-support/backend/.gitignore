node_modules/
dist/
.env
*.log

# Real data pasted in for the one-off bulk-load script (scripts/bulk-load.ts).
# The committed reference is scripts/schemes-batch.example.json.
scripts/schemes-batch.json

# Prisma writes the generated client into node_modules/@prisma/client
# (regenerated by `npm run prisma:generate`). Migrations under prisma/migrations
# ARE tracked once you start creating them.
